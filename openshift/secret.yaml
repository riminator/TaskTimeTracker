apiVersion: v1
kind: Secret
metadata:
  name: timetracker-secrets
type: Opaque
stringData:
  # Optional: Only needed if using Microsoft Graph integration
  # Leave empty if using manual/hybrid mode without Azure AD
  tenant-id: ""
  client-id: ""
  client-secret: ""

# Made with Bob
