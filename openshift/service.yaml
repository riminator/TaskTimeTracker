apiVersion: v1
kind: Service
metadata:
  name: teams-timetracker
  labels:
    app: teams-timetracker
spec:
  type: ClusterIP
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: teams-timetracker

# Made with Bob
