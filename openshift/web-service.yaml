apiVersion: v1
kind: Service
metadata:
  name: timetracker-web
  labels:
    app: timetracker-web
    component: frontend
spec:
  type: ClusterIP
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: timetracker-web

# Made with Bob
